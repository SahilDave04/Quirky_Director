[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "director"
version = "1.0.0"
description = "A locally developed utility package"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []